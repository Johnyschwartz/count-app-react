import React from "react"
import ReactDom from "react-dom/client"
import "./style.css"
import Com from "./counter app"
const root = ReactDom.createRoot(document.getElementById("root"))
root.render(
  <Com></Com>
)